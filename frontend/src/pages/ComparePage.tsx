export function ComparePage() {
    return (
        <div style={{ padding: 48, textAlign: "center", color: "var(--text-muted)" }}>
            <div style={{ fontSize: 14 }}>Comparador — próximamente</div>
        </div>
    );
}